# Public env vars are inlined into the Expo bundle. Anything starting with
# EXPO_PUBLIC_ is exposed to the client — do NOT put secrets here.

# The Vercel deployment that hosts /api/flight. Used only on native (iOS /
# Android via Expo Go); on web the relative URL works.
EXPO_PUBLIC_API_BASE=https://your-vercel-deployment.vercel.app
